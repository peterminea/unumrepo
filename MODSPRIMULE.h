void SUSMODSPRIMULE4BIF1(int *M, int W, int &u, gmp_ui *b, gmp_ui *b1, mpz_t *C, mpz_t *C1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A2, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, gmp_ui *sum2, int D, int E, int H, int K, int j, char *Q, FILE *F2, int g, mpz_t p, mpz_t q) {
	while(1) {
		if(!W--) break;
		if(mpz_divisible_ui_p(p, A2[W])) {
			if(!u) {
				SUMDIVV1(p, d, C, C1, b, b1, D, E, H, K, g, div1, sum1, div, sum, mem, q, 0);
				u = 1;
				}				
					
			SUMINT1(p, unu, trei, A2[W], 1+A2[W], sum2[W], d, d1, doi, Q, F2);
			}
		}
}


void SUSMODSPRIMULE4BIF2(int *M0, int *M1, int W0, int u, gmp_ui *b, gmp_ui *b1, mpz_t *C, mpz_t *C1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A20, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, gmp_ui *sum2, int D, int E, int H, int K, int j, char *Q, FILE *F2, int g, mpz_t p, mpz_t q, int h) {
	while(1) {
		if(!W0--) break;
		if(mpz_divisible_ui_p(p, A20[W0])) {
			if(!u && !h) {
				SUMDIVV1(p, d, C, C1, b, b1, D, E, H, K, g, div1, sum1, div, sum, mem, q, 0);
				u = 1;
				}
						
			SUMMPZE(p, unu, trei, A20[W0], mem[M1[W0]], d, d1, doi, div1[M1[W0], sum1[M1[W0], Q, F2);
			}
		}
}


void SUSMODSPRIMULE4BIF3(int *M, int W, int &u, gmp_ui *b, gmp_ui *b1, mpz_t *C, mpz_t *C1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A2, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, gmp_ui *sum2, int D, int E, int H, int K, int j, char *Q, FILE *F2, mpz_t p, mpz_t *q) {
	while(1) {
		if(!W--) break;
		if(mpz_divisible_ui_p(unu, A2[W])) {
			if(!u) {
				mpz_tdiv_q_2exp(p, unu, u = mpz_scan1(unu, 0));
				SUMDIVV1(p, d, C, C1, b, b1, D, E, H, K, u, div1, sum1, div, sum, mem, q[u], 0);
				}				
			
			SUMINT1(p, unu, trei, A2[W], 1+A2[W], sum2[W], d, d1, doi, Q, F2);
			}
		}
}


void SUSMODSPRIMULE4BIF4(int *M0, int *M1, int W0, int u, gmp_ui *b, gmp_ui *b1, mpz_t *C, mpz_t *C1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A20, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, gmp_ui *sum2, int D, int E, int H, int K, int j, char *Q, FILE *F2, mpz_t p, mpz_t *q, int h) {
	while(1) {
		if(!W0--) break;
		if(mpz_divisible_ui_p(unu, A20[W0])) {
			if(!u && !h) {
				mpz_tdiv_q_2exp(p, unu, u = mpz_scan1(unu, 0));
				SUMDIVV1(p, d, C, C1, b, b1, D, E, H, K, u, div1, sum1, div, sum, mem, q[u], 0);
				}				
			
			SUMMPZE(p, unu, trei, A20[W0], mem[M1[W0]], d, d1, doi, div1[M1[W0], sum1[M1[W0], Q, F2);
			}
		}
}


void SUSMODSPRIMULE5BIF1(int *M, int W, int &u, gmp_ui *b, gmp_ui *b1, mpz_t *C, mpz_t *C1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A2, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, gmp_ui *sum2, int D, int E, int H, int K, int j, char *Q, FILE *F2, int g, mpz_t p, mpz_t q) {
	while(1) {
		if(!W--) break;
		if(mpz_divisible_ui_p(p, A2[W])) {
			if(!u) {
				SUMDIVV(p, d, C, C1, b, b1, D, E, H, K, g, div1, sum1, div, sum, mem, q);
				u = 1;
				}				
			
			SUMINT1(p, unu, trei, A2[W], 1+A2[W], sum2[W], d, d1, doi, Q, F2);
			}
		}
}


void SUSMODSPRIMULE5BIF2(int *M0, int *M1, int W0, int u, gmp_ui *b, gmp_ui *b1, mpz_t *C, mpz_t *C1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A20, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, gmp_ui *sum2, int D, int E, int H, int K, int j, char *Q, FILE *F2, int g, mpz_t p, mpz_t q, int h) {
	while(1) {
		if(!W0--) break;
		if(mpz_divisible_ui_p(p, A20[W0])) {
			if(!u && !h) {
				SUMDIVV(p, d, C, C1, b, b1, D, E, H, K, g, div1, sum1, div, sum, mem, q);
				u = 1;
				}				
			
			SUMMPZE(p, unu, trei, A20[W0], mem[M1[W0]], d, d1, doi, div1[M1[W0], sum1[M1[W0], Q, F2);
			}
		}
}


void SUSMODSPRIMULE5BIF3(int *M, int W, int &u, gmp_ui *b, gmp_ui *b1, mpz_t *C, mpz_t *C1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A2, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, gmp_ui *sum2, int D, int E, int H, int K, int j, char *Q, FILE *F2, mpz_t p, mpz_t *q) {
	while(1) {
		if(!W--) break;
		if(mpz_divisible_ui_p(unu, A2[W])) {
			if(!u) {
				mpz_tdiv_q_2exp(p, unu, u = mpz_scan1(unu, 0));
				SUMDIVV(p, d, C, C1, b, b1, D, E, H, K, u, div1, sum1, div, sum, mem, q[u]);
				}
			
			SUMINT1(p, unu, trei, A2[W], 1+A2[W], sum2[W], d, d1, doi, Q, F2);
			}
		}
}


void SUSMODSPRIMULE5BIF4(int *M0, int *M1, int W0, int u, gmp_ui *b, gmp_ui *b1, mpz_t *C, mpz_t *C1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A20, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, gmp_ui *sum2, int D, int E, int H, int K, int j, char *Q, FILE *F2, mpz_t p, mpz_t *q, int h) {
	while(1) {
		if(!W0--) break;
		if(mpz_divisible_ui_p(unu, A20[W0])) {
			if(!u && !h) {
				mpz_tdiv_q_2exp(p, unu, u = mpz_scan1(unu, 0));
				SUMDIVV(p, d, C, C1, b, b1, D, E, H, K, u, div1, sum1, div, sum, mem, q[u]);
				}				
			
			SUMMPZE(p, unu, trei, A20[W0], mem[M1[W0]], d, d1, doi, div1[M1[W0], sum1[M1[W0], Q, F2);
			}
		}
}


void SUSMODSPRIMULE6BIF1(int *M, int W, int &u, gmp_ui *b, gmp_ui *b1, mpz_t *C, mpz_t *C1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A2, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, gmp_ui *sum2, int D, int E, int H, int K, int j, char *Q, FILE *F2, int g, mpz_t p, mpz_t q) {
	while(1) {
		if(!W--) break;
		if(mpz_divisible_ui_p(p, A2[W])) {
			if(!u) {
				SUMIMENS1(p, d, C, C1, b, b1, D, E, H, K, g, div1, sum1, div, sum, mem, q);
				u = 1;
				}
			
			SUMINT1(p, unu, trei, A2[W], 1+A2[W], sum2[W], d, d1, doi, Q, F2);
			}
		}
}


void SUSMODSPRIMULE6BIF2(int *M0, int *M1, int W0, int u, gmp_ui *b, gmp_ui *b1, mpz_t *C, mpz_t *C1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A20, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, gmp_ui *sum2, int D, int E, int H, int K, int j, char *Q, FILE *F2, int g, mpz_t p, mpz_t q, int h) {
	while(1) {
		if(!W0--) break;
		if(mpz_divisible_ui_p(p, A20[W0])) {
			if(!u && !h) {
				SUMIMENS1(p, d, C, C1, b, b1, D, E, H, K, g, div1, sum1, div, sum, mem, q);
				u = 1;
				}				
			
			SUMMPZE(p, unu, trei, A20[W0], mem[M1[W0]], d, d1, doi, div1[M1[W0], sum1[M1[W0], Q, F2);
			}
		}
}


void SUSMODSPRIMULE6BIF3(int *M, int W, int &u, gmp_ui *b, gmp_ui *b1, mpz_t *C, mpz_t *C1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A2, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, gmp_ui *sum2, int D, int E, int H, int K, int j, char *Q, FILE *F2, mpz_t p, mpz_t *q) {
	while(1) {
		if(!W--) break;
		if(mpz_divisible_ui_p(unu, A2[W])) {
			if(!u) {
				mpz_tdiv_q_2exp(p, unu, u = mpz_scan1(unu, 0));
				SUMIMENS1(p, d, C, C1, b, b1, D, E, H, K, u, div1, sum1, div, sum, mem, q[u]);
				}				
			
			SUMINT1(p, unu, trei, A2[W], 1+A2[W], sum2[W], d, d1, doi, Q, F2);
			}
		}
}


void SUSMODSPRIMULE6BIF4(int *M0, int *M1, int W0, int u, gmp_ui *b, gmp_ui *b1, mpz_t *C, mpz_t *C1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A20, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, gmp_ui *sum2, int D, int E, int H, int K, int j, char *Q, FILE *F2, mpz_t p, mpz_t *q, int h) {
	while(1) {
		if(!W0--) break;
		if(mpz_divisible_ui_p(unu, A20[W0])) {
			if(!u && !h) {
				mpz_tdiv_q_2exp(p, unu, u = mpz_scan1(unu, 0));
				SUMIMENS1(p, d, C, C1, b, b1, D, E, H, K, u, div1, sum1, div, sum, mem, q[u]);
				}				
			
			SUMMPZE(p, unu, trei, A20[W0], mem[M1[W0]], d, d1, doi, div1[M1[W0], sum1[M1[W0], Q, F2);
			}
		}
}


void SUSMODSPRIMULE7BIF1(int *M, int W, int &u, gmp_ui *b, gmp_ui *b1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A2, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, gmp_ui *sum2, int D, int H, int K, int j, char *Q, FILE *F2, int g, mpz_t p, mpz_t q) {
	while(1) {
		if(!W--) break;
		if(mpz_divisible_ui_p(p, A2[W])) {
			if(!u) {
				NUMESUMV(p, d, b, b1, D, H, K, g, div1, sum1, div, sum, mem, q, 0);
				u = 1;
				}				
			
			SUMINT1(p, unu, trei, A2[W], 1+A2[W], sum2[W], d, d1, doi, Q, F2);
			}
		}
}


void SUSMODSPRIMULE7BIF2(int *M0, int *M1, int W0, int u, gmp_ui *b, gmp_ui *b1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A20, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, gmp_ui *sum2, int D, int H, int K, int j, char *Q, FILE *F2, int g, mpz_t p, mpz_t q, int h) {
	while(1) {
		if(!W0--) break;
		if(mpz_divisible_ui_p(p, A20[W0])) {
			if(!u && !h) {
				NUMESUMV(p, d, b, b1, D, H, K, g, div1, sum1, div, sum, mem, q, 0);
				u = 1;
				}				
			
			SUMMPZE(p, unu, trei, A20[W0], mem[M1[W0]], d, d1, doi, div1[M1[W0], sum1[M1[W0], Q, F2);
			}
		}
}


void SUSMODSPRIMULE7BIF3(int *M, int W, int &u, gmp_ui *b, gmp_ui *b1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A2, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, gmp_ui *sum2, int D, int H, int K, int j, char *Q, FILE *F2, mpz_t p, mpz_t *q) {
	while(1) {
		if(!W--) break;
		if(mpz_divisible_ui_p(unu, A2[W])) {
			if(!u) {
				mpz_tdiv_q_2exp(p, unu, u = mpz_scan1(unu, 0));
				NUMESUMV(p, d, b, b1, D, H, K, u, div1, sum1, div, sum, mem, q[u], 0);				
				}				
			
			SUMINT1(p, unu, trei, A2[W], 1+A2[W], sum2[W], d, d1, doi, Q, F2);
			}
		}
}


void SUSMODSPRIMULE7BIF4(int *M0, int *M1, int W0, int u, gmp_ui *b, gmp_ui *b1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A20, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, gmp_ui *sum2, int D, int H, int K, int j, char *Q, FILE *F2, mpz_t p, mpz_t *q, int h) {
	while(1) {
		if(!W0--) break;
		if(mpz_divisible_ui_p(unu, A20[W0])) {
			if(!u && !h) {
				mpz_tdiv_q_2exp(p, unu, u = mpz_scan1(unu, 0));
				NUMESUMV(p, d, b, b1, D, H, K, u, div1, sum1, div, sum, mem, q[u], 0);
				}				
			
			SUMMPZE(p, unu, trei, A20[W0], mem[M1[W0]], d, d1, doi, div1[M1[W0], sum1[M1[W0], Q, F2);
			}
		}
}


//MODSPRIMULE：
void SUSMODSPRIMULE1(int *M, char *Q, long PP2, FILE *f1, int W, int f, int u, gmp_ui *b, gmp_ui *b1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A2, gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int D, int H, int j, FILE *F2, gmp_ui *sum2, int g, mpz_t p, gmp_ui *q) {
if(W > PRAGW)
	while(1) {
		mpz_set_str(unu, Q, 10);
		f = W, u = 0;
		mpz_tdiv_q_2exp(p, unu, g = mpz_scan1(unu, 0));
		
		while(1) {
			if(!f--) break;
			if(mpz_divisible_ui_p(p, A2[f])) {
				if(!u) {
					SMV(p, d, b, b1, D, H, g, div1, sum1, q[g]);
					u = 1;
					}
				
				SUMINT1(p, unu, trei, A2[f], 1+A2[f], sum2[f], d, d1, doi, Q, F2);
				}
			}
		if(!(ftell(f1) ^ PP2))
			break;
		fgets(Q, 1920, f1);
		}
else
	while(1) {
		mpz_set_str(unu, Q, 10);
		f = W, u = 0;
		
		while(1) {
			if(!f--) break;
			if(mpz_divisible_ui_p(unu, A2[f])) {
				if(!u) {					
					mpz_tdiv_q_2exp(p, unu, g = mpz_scan1(unu, 0));
					SMV(p, d, b, b1, D, H, g, div1, sum1, q[g]);
					u = 1;
					}

				
				SUMINT1(p, unu, trei, A2[f], 1+A2[f], sum2[f], d, d1, doi, Q, F2);
				}
			}
		if(!(ftell(f1) ^ PP2))
			break;
		fgets(Q, 1920, f1);
	}
}


void SUSMODSPRIMULE2(int *M, char *Q, long PP2, FILE *f1, int W, int f, int u, gmp_ui *b, gmp_ui *b1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A2, gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int D, int H, int j, FILE *F2, gmp_ui *sum2, int g, mpz_t p, mpz_t *q) {
if(W > PRAGW)
	while(1) {
		mpz_set_str(unu, Q, 10);
		f = W, u = 0;
		mpz_tdiv_q_2exp(p, unu, g = mpz_scan1(unu, 0));
		
		while(1) {
			if(!f--) break;
			if(mpz_divisible_ui_p(p, A2[f])) {
				if(!u) {
					NUMSUMPURG1(p, d, b, b1, D, H, g, div1, sum1, q[g], 0);
					u = 1;
					}
					
				
				SUMINT1(p, unu, trei, A2[f], 1+A2[f], sum2[f], d, d1, doi, Q, F2);
				}
			}
		if(!(ftell(f1) ^ PP2))
			break;
		fgets(Q, 1920, f1);
		}
else
	while(1) {
		mpz_set_str(unu, Q, 10);
		f = W, u = 0;
		
		while(1) {
			if(!f--) break;
			if(mpz_divisible_ui_p(unu, A2[f])) {
				if(!u) {
					mpz_tdiv_q_2exp(p, unu, g = mpz_scan1(unu, 0));
					NUMSUMPURG1(p, d, b, b1, D, H, g, div1, sum1, q[g], 0);
					u = 1;
					}
					
				
				SUMINT1(p, unu, trei, A2[f], 1+A2[f], sum2[f], d, d1, doi, Q, F2);
				}
			}
		if(!(ftell(f1) ^ PP2))
			break;
		fgets(Q, 1920, f1);
	}
}


void SUSMODSPRIMULE3(int *M, char *Q, long PP2, FILE *f1, int W, int f, int u, gmp_ui *b, gmp_ui *b1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A2, gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int D, int H, int j, FILE *F2, gmp_ui *sum2, int g, mpz_t p, mpz_t *q) {
if(W > PRAGW)
	while(1) {
		mpz_set_str(unu, Q, 10);
		f = W, u = 0;
		mpz_tdiv_q_2exp(p, unu, g = mpz_scan1(unu, 0));
		
		while(1) {
			if(!f--) break;
			if(mpz_divisible_ui_p(p, A2[f])) {
				if(!u) {
					NUMSUMV1(p, d, b, b1, D, H, g, div1, sum1, q[g], 0);
					u = 1;
					}
					
				
				SUMINT1(p, unu, trei, A2[f], 1+A2[f], sum2[f], d, d1, doi, Q, F2);
				}
			}
		if(!(ftell(f1) ^ PP2))
			break;
		fgets(Q, 1920, f1);
		}
else
	while(1) {
		mpz_set_str(unu, Q, 10);
		f = W, u = 0;
		
		while(1) {
			if(!f--) break;
			if(mpz_divisible_ui_p(unu, A2[f])) {
				if(!u) {
					mpz_tdiv_q_2exp(p, unu, g = mpz_scan1(unu, 0));
					NUMSUMV1(p, d, b, b1, D, H, g, div1, sum1, q[g], 0);
					u = 1;
					}
					
				
				SUMINT1(p, unu, trei, A2[f], 1+A2[f], sum2[f], d, d1, doi, Q, F2);
				}
			}
		if(!(ftell(f1) ^ PP2))
			break;
		fgets(Q, 1920, f1);
	}
}


void SUSMODSPRIMULE4(int *M, int *M0, char *Q, long PP2, FILE *f1, int W, int W0, gmp_ui *b, gmp_ui *b1, mpz_t *C, mpz_t *C1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A2, gmp_ui *A20, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, int D, int E, int H, int K, FILE *F2, gmp_ui *sum2, int g, int h, mpz_t p, mpz_t *q) {
//PURGANTE(C, C1, E);
		
if(W > PRAGW)
	while(1) {
		mpz_set_str(unu, Q, 10);
		mpz_tdiv_q_2exp(p, unu, g = mpz_scan1(unu, 0));
		SUSMODSPRIMULE4BIF1(M, W, h = 0, b, b1, C, C1, unu, doi, trei, d, d1, A2, div, sum, div1, sum1, mem, sum2, D, E, H, K, 0, Q, F2, g, p, q[g]);
		//SUSMODSPRIMULE4BIF2(M0, W0, 0, b, b1, C, C1, unu, doi, trei, d, d1, A20, div, sum, div1, sum1, mem, D, E, H, K, 0, Q, F2, g, p, q[g], h);
		if(!(ftell(f1) ^ PP2))
			break;
		fgets(Q, 1920, f1);
		}
else
	while(1) {
		mpz_set_str(unu, Q, 10);
		SUSMODSPRIMULE4BIF3(M, W, h = 0, b, b1, C, C1, unu, doi, trei, d, d1, A2, div, sum, div1, sum1, mem, sum2, D, E, H, K, 0, Q, F2, p, q);
		//SUSMODSPRIMULE4BIF4(M0, W0, 0, b, b1, C, C1, unu, doi, trei, d, d1, A20, div, sum, div1, sum1, mem, D, E, H, K, 0, Q, F2, p, q, h);
		if(!(ftell(f1) ^ PP2))
			break;
		fgets(Q, 1920, f1);
		}
}


void SUSMODSPRIMULE5(int *M, int *M0, char *Q, long PP2, FILE *f1, int W, int W0, gmp_ui *b, gmp_ui *b1, mpz_t *C, mpz_t *C1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A2, gmp_ui *A20, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, int D, int E, int H, int K, FILE *F2, gmp_ui *sum2, int g, int h, mpz_t p, mpz_t *q) {
		
if(W > PRAGW)
	while(1) {
		mpz_set_str(unu, Q, 10);
		mpz_tdiv_q_2exp(p, unu, g = mpz_scan1(unu, 0));
		SUSMODSPRIMULE5BIF1(M, W, h = 0, b, b1, C, C1, unu, doi, trei, d, d1, A2, div, sum, div1, sum1, mem, sum2, D, E, H, K, 0, Q, F2, g, p, q[g]);
		//SUSMODSPRIME5BIF2(M0, W0, 0, b, b1, C, C1, unu, doi, trei, d, d1, A20, div, sum, div1, sum1, mem, D, E, H, K, 0, Q, F2, g, p, q[g], h);
		if(!(ftell(f1) ^ PP2))
			break;
		fgets(Q, 1920, f1);
		}
else
	while(1) {
		mpz_set_str(unu, Q, 10);
		SUSMODSPRIMULE5BIF3(M, W, h = 0, b, b1, C, C1, unu, doi, trei, d, d1, A2, div, sum, div1, sum1, mem, sum2, D, E, H, K, 0, Q, F2, p, q);
		//SUSMODSPRIME5BIF4(M0, W0, 0, b, b1, C, C1, unu, doi, trei, d, d1, A20, div, sum, div1, sum1, mem, D, E, H, K, 0, Q, F2, p, q, h);
		if(!(ftell(f1) ^ PP2))
			break;
		fgets(Q, 1920, f1);
		}
}


void SUSMODSPRIMULE6(int *M, int *M0, char *Q, long PP2, FILE *f1, int W, int W0, gmp_ui *b, gmp_ui *b1, mpz_t *C, mpz_t *C1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A2, gmp_ui *A20, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, int D, int E, int H, int K, FILE *F2, gmp_ui *sum2, int g, int h, mpz_t p, mpz_t *q) {
		
if(W > PRAGW)
	while(1) {
		mpz_set_str(unu, Q, 10);
		mpz_tdiv_q_2exp(p, unu, g = mpz_scan1(unu, 0));
		SUSMODSPRIMULE6BIF1(M, W, h = 0, b, b1, C, C1, unu, doi, trei, d, d1, A2, div, sum, div1, sum1, mem, sum2, D, E, H, K, 0, Q, F2, g, p, q[g]);
		//SUSMODSPRIMULE6BIF2(M0, W0, 0, b, b1, C, C1, unu, doi, trei, d, d1, A20, div, sum, div1, sum1, mem, D, E, H, K, 0, Q, F2, g, p, q[g], h);
		if(!(ftell(f1) ^ PP2))
			break;
		fgets(Q, 1920, f1);
		}
else
	while(1) {
		mpz_set_str(unu, Q, 10);
		SUSMODSPRIMULE6BIF3(M, W, h = 0, b, b1, C, C1, unu, doi, trei, d, d1, A2, div, sum, div1, sum1, mem, sum2, D, E, H, K, 0, Q, F2, p, q);
		//SUSMODSPRIMULE6BIF4(M0, W0, 0, b, b1, C, C1, unu, doi, trei, d, d1, A20, div, sum, div1, sum1, mem, D, E, H, K, 0, Q, F2, p, q, h);
		if(!(ftell(f1) ^ PP2))
			break;
		fgets(Q, 1920, f1);
		}
}


void SUSMODSPRIMULE7(int *M, int *M0, char *Q, long PP2, FILE *f1, int W, int W0, gmp_ui *b, gmp_ui *b1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A2, gmp_ui *A20, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, int D, int H, int K, FILE *F2, gmp_ui *sum2, int g, int h, mpz_t p, mpz_t *q) {
		
if(W > PRAGW)
	while(1) {
		mpz_set_str(unu, Q, 10);
		mpz_tdiv_q_2exp(p, unu, g = mpz_scan1(unu, 0));
		SUSMODSPRIMULE7BIF1(M, W, h = 0, b, b1, unu, doi, trei, d, d1, A2, div, sum, div1, sum1, mem, sum2, D, H, K, 0, Q, F2, g, p, q[g]);
		//SUSMODSPRIMULE7BIF2(M0, W0, 0, b, b1, unu, doi, trei, d, d1, A20, div, sum, div1, sum1, mem, D, H, K, 0, Q, F2, g, p, q[g], h);
		if(!(ftell(f1) ^ PP2))
			break;
		fgets(Q, 1920, f1);
		}
else
	while(1) {
		mpz_set_str(unu, Q, 10);
		SUSMODSPRIMULE7BIF3(M, W, h = 0, b, b1, unu, doi, trei, d, d1, A2, div, sum, div1, sum1, mem, sum2, D, H, K, 0, Q, F2, p, q);
		//SUSMODSPRIMULE7BIF4(M0, W0, 0, b, b1, unu, doi, trei, d, d1, A20, div, sum, div1, sum1, mem, D, H, K, 0, Q, F2, p, q, h);
		if(!(ftell(f1) ^ PP2))
			break;
		fgets(Q, 1920, f1);
		}
}


void SUSMODSPRIMULE8(int *M, char *Q, long PP2, FILE *f1, int W, int f, int u, gmp_ui *b, gmp_ui *b1, mpz_t *C, mpz_t *C1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A2, gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int D, int E, int H, int j, FILE *F2, gmp_ui *sum2, int g, mpz_t p, mpz_t *q) {
if(W > PRAGW)
	while(1) {
		mpz_set_str(unu, Q, 10);
		f = W, u = 0;
		mpz_tdiv_q_2exp(p, unu, g = mpz_scan1(unu, 0));
		
		while(1) {
			if(!f--) break;
			if(mpz_divisible_ui_p(p, A2[f])) {
				if(!u) {
					NUMSUMV2(p, d, C, C1, b, b1, D, E, H, g, div1, sum1, q[g], 0);
					u = 1;
					}
					
				
				SUMINT1(p, unu, trei, A2[f], 1+A2[f], sum2[f], d, d1, doi, Q, F2);
				}
			}
		if(!(ftell(f1) ^ PP2))
			break;
		fgets(Q, 1920, f1);
		}
else
	while(1) {
		mpz_set_str(unu, Q, 10);
		f = W, u = 0;
		
		while(1) {
			if(!f--) break;
			if(mpz_divisible_ui_p(unu, A2[f])) {
				if(!u) {
					mpz_tdiv_q_2exp(p, unu, g = mpz_scan1(unu, 0));
					NUMSUMV2(p, d, C, C1, b, b1, D, E, H, g, div1, sum1, q[g], 0);
					u = 1;
					}
					
				
				SUMINT1(p, unu, trei, A2[f], 1+A2[f], sum2[f], d, d1, doi, Q, F2);
				}
			}
		if(!(ftell(f1) ^ PP2))
			break;
		fgets(Q, 1920, f1);
	}
}


void MODSPRIMULE(char *Q, long PP2, FILE *f1, int W, int f, int *M, int *M0, int W0, int f0, gmp_ui *b, gmp_ui *b1, mpz_t *C, mpz_t *C1, mpz_t unu, mpz_t doi, mpz_t trei, mpz_t d, mpz_t d1, gmp_ui *A2, gmp_ui *AP, gmp_ui *A20, gmp_ui *AP0, mpz_t div[][MAX1], mpz_t sum[][MAX1], gmp_ui div1[][MAX2], gmp_ui sum1[][MAX2], int *mem, gmp_ui *sum2, int D, int E, int H, int K, FILE *F2, int &sus, int dir, mpz_t p, mpz_t *q, gmp_ui *q0) {
	if(dir) {
		sus < 55? SUSMODSPRIMULE1(M, Q, PP2, f1, W = f, f, 0, b, b1, unu, doi, trei, d, d1, A2, div1, sum1, D, H, 0, F2, sum2, 0, p, q0) 
		: sus < 73? SUSMODSPRIMULE2(M, Q, PP2, f1, W = f, f, 0, b, b1, unu, doi, trei, d, d1, A2, div1, sum1, D, H, 0, F2, sum2, 0, p, q) 
			: sus < 213? SUSMODSPRIMULE3(M, Q, PP2, f1, W = f, f, 0, b, b1, unu, doi, trei, d, d1, A2, div1, sum1, D, H, 0, F2, sum2, 0, p, q)
			: sus < 223? SUSMODSPRIMULE8(M, Q, PP2, f1, W = f, f, 0, b, b1, C, C1, unu, doi, trei, d, d1, A2, div1, sum1, D, E, H, 0, F2, sum2, 0, p, q) 
				: sus < 243? SUSMODSPRIMULE4(M, M0, Q, PP2, f1, W = f, W0 = f0, b, b1, C, C1, unu, doi, trei, d, d1, A2, A20, div, sum, div1, sum1, mem, D, E, H, K, F2, sum2, 0, 0, p, q) 
					: sus < 503? SUSMODSPRIMULE5(M, M0, Q, PP2, f1, W = f, W0 = f0, b, b1, C, C1, unu, doi, trei, d, d1, A2, A20, div, sum, div1, sum1, mem, D, E, H, K, F2, sum2, 0, 0, p, q)
					: SUSMODSPRIMULE6(M, M0, Q, PP2, f1, W = f, W0 = f0, b, b1, C, C1, unu, doi, trei, d, d1, A2, A20, div, sum, div1, sum1, mem, D, E, H, K, F2, sum2, 0, 0, p, q);
		}
}